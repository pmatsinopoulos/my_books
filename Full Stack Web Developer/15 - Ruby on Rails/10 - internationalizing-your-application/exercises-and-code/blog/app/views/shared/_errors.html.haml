- # File: app/views/shared/_errors.html.haml
- #
- # Local Variables:
- #
- # model {ActiveModel::Mode}. This should be an ActiveModel::Model that responds to +#errors+
- # explanation {String}. The text that will appear next to the number of errors
- #
- if model.errors.any?
  #error-explanation.alert.alert-danger.alert-dismissible{role: 'alert'}
    %button.close{'aria-label': 'Close', 'data-dismiss': 'alert', type: 'button'}
      %span{'aria-hidden': 'true'} ×
    %h2
      = pluralize(model.errors.count, 'error')
      = explanation
    %ul
      - model.errors.full_messages.each do |msg|
        %li= msg
