## Question #1: Which CSS property and which value allows a block-level element to stay on the same like with other block-level elements as if it were an inline element?
 
*Multiple Choice*

1. `display: block`
2. `inline: true`
3. `inline: block`
4. `display: inline-block`

